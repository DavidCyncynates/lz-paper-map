# Used only when generating absolute social-card and canonical URLs.
NEXT_PUBLIC_SITE_URL=https://davidcyncynates.github.io/lz-paper-map/

# Required by the scheduled paper-screening workflow.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
